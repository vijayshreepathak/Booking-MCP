services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: appointment_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/appointment_db
      BASE_URL: http://localhost:8000
      MCP_SERVER_URL: http://mcp-server:8100/mcp
      LLM_PROVIDER: demo
    depends_on:
      postgres:
        condition: service_healthy
      mcp-server:
        condition: service_started
    command: >
      sh -c "
        python scripts/seed_db.py &&
        uvicorn app.main:app --host 0.0.0.0 --port 8000
      "

  mcp-server:
    build: ./backend
    ports:
      - "8100:8100"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/appointment_db
      BASE_URL: http://localhost:8000
    depends_on:
      postgres:
        condition: service_healthy
    command: >
      sh -c "
        python scripts/seed_db.py &&
        uvicorn app.mcp_server_app:app --host 0.0.0.0 --port 8100
      "

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    environment:
      VITE_API_URL: http://localhost:8000

volumes:
  pgdata:
